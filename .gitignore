# 插件自己的 node_modules 是 junction（指回 profile 依赖表），无本地副本可提交。
node_modules/

# host 半身的 source map 不发布也不入库（Node 不带 --enable-source-maps 就不读，
# 13.6MB 白占工作区和 git 历史）；client 的 map 保留给浏览器 DevTools。
lib/index.js.map

# 注意：**不要**忽略 lib/ —— 构建产物必须随仓库提交：
# pnpm>=10 拦截 git 依赖的 prepare 脚本，安装方不跑构建，
# lib/index.js + lib/client.js 就是交付物。
